shrink = 1.3
gap = size
swapped = true

while(swapped || gap > 1){
  gap = (int)(gap / size) 

  if(gap > 1){
    gap = 1
  }

  swapped = false

  for(int i = 0; i + gap < size; i++){
    if(arr[i] > arr[i + gap]) 
      swap(arr[i], arr[i + gap]
      swapped = true
